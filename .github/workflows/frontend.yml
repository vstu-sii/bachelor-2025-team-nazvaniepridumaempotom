name: Frontend CI

on:
  pull_request:
  push:
    branches: [MLOPS]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: |
          cd frontend
          npm install --legacy-peer-deps  # ← Измените здесь

      - name: Lint
        run: |
          cd frontend
          npm run lint

      - name: Tests
        run: |
          cd frontend
          npm test